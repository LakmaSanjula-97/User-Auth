import { Button } from '@chakra-ui/react';
import React from 'react';

const Logout = () => {
  return (
    <div>
        <div>
            {/* <Button onClick={() => {
                localStorage.clear()
                dispatchEvent({
                    type:"CLEAR"
                }
            )
        }}>
          Log Out
        </Button> */}
      </div>
    </div>
  );
}

export default Logout;
